# Copy to web/.env for local `npm run dev` outside Docker. Inside Docker the
# compose `web` service sets VITE_API_URL automatically.
VITE_API_URL=http://localhost:8081
# Optional: prefill the tenant id on the M0 skeleton login screen.
VITE_DEV_TENANT_ID=
